This is a Shortcut for connect OSU VPN use cisco

user need to install pandas: 
pip install pandas

pyautogui: 
pip install pyautogui

pyperclip:
pip install pyperclip

image much be .png form

run:
python3 main_macro.py


note: my computer is mac. There are some different between mac's hotkeys and windows' hotkeys.

